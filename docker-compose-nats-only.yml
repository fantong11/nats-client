version: '3.8'

services:
  nats:
    image: nats:2.10.7-alpine
    container_name: nats-server
    ports:
      - "14222:4222"  # NATS client port
      - "18222:8222"  # HTTP monitoring port
    command:
      - "--http_port"
      - "8222"
      - "--name"
      - "nats-server"
      - "--log"
      - "/dev/stdout"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8222/varz"]
      interval: 30s
      timeout: 10s
      retries: 3